{
  "name": "public-directory-portfolio",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "npm run build -w deeptube && npm run build -w osee && npm run build -w country-data",
    "build:osee": "npm run build -w osee",
    "build:country-data": "npm run build -w country-data",
    "build:deeptube": "npm run build -w deeptube",
    "deploy": "npm-run-all build:deeptube build:osee build:country-data"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1"
  },
  "workspaces": [
    "deeptube",
    "osee",
    "country-data"
  ]
}
