name: Deploy Website

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: 'npm'

      - name: Clear npm cache
        run: npm cache clean --force

      - name: Install dependencies
        run: npm ci

      - name: Build all projects
        run: npm run build

      - name: Create deployment directory
        run: |
          mkdir -p deploy
          cp -r deeptube/build/. deploy/deeptube/
          cp -r dictator/build/. deploy/dictator/
          cp -r meeting-agent/build/. deploy/meeting-agent/
          cp -r osee/build/. deploy/osee/
          cp -r country-data/build/. deploy/country-data/
          mkdir -p deploy/country-data/data
          cp -r country-data/public/data/* deploy/country-data/data/
          cp -r cv deploy/
          cp -r business-data deploy/
          cp -r companies-data deploy/
          cp -r event-hunter deploy/
          cp -r israel-iran-war deploy/
          cp index.html deploy/
          cp 404.html deploy/

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: ./deploy

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 